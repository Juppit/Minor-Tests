language: c
jobs:
  include:
    - os: windows
      install:
        - ls -l /mingw64
        - ls -l /mingw64/bin
        - ls -l /usr/bin
        - ls -l /bin
        - ls -l /c/
        - ls -l /c/ProgramData/chocolatey/bin
        - ls -l /c/ProgramData/chocolatey/lib/mingw/tools/install/mingw64/bin
        - ls -l '/c/Program Files/git/usr/bin'
        - ln -s /c/ProgramData/chocolatey/lib/mingw/tools/install/mingw64/bin/mingw32-make.exe /usr/bin/make.exe
        # set Path needed for wget and unzip
        #- PATH="/bin:/usr/bin:/c/ProgramData/chocolatey/bin:/c/Program Files/git/usr/bin"
        - wget "https://netcologne.dl.sourceforge.net/project/mingw/MinGW/Extension/bsdtar/basic-bsdtar-2.8.3-1/basic-bsdtar-2.8.3-1-mingw32-bin.zip" >>/dev/null
        - unzip basic-bsdtar-2.8.3-1-mingw32-bin.zip >>/dev/null
        - mv basic-bsdtar.exe /usr/bin/bsdtar.exe >>/dev/null
        - wget "https://netcologne.dl.sourceforge.net/project/gnuwin32/m4/1.4.14-1/m4-1.4.14-1-bin.zip" >>/dev/null
        - unzip m4-1.4.14-1-bin.zip >>/dev/null
        - mv bin/m4* /usr/bin >>/dev/null
        - wget "https://datapacket.dl.sourceforge.net/project/gnuwin32/m4/1.4.14-1/m4-1.4.14-1-dep.zip" >>/dev/null
        - unzip m4-1.4.14-1-dep.zip >>/dev/null
        - mv bin/regex* /usr/bin >>/dev/null
        #- cp /c/ProgramData/chocolatey/bin/mingw32-make.exe /usr/bin/make.exe
        - ls -l /usr/bin
      script:
        #- CC=/c/ProgramData/chocolatey/lib/mingw/tools/install/mingw64/bin/gcc.exe
        # set PATH for the shell executing our Makefile
        - mv '/c/program files/Git/usr/bin/sh.exe' /usr/bin
        - ls -l /usr/bin/s*
        - rm -f '/c/program files/Git/usr/bin/sh.exe'
        #- where sh.exe
        - ls -l '/c/Program Files/git/usr/bin/s*'
        - export PATH='/bin:/usr/bin:/c/ProgramData/chocolatey/bin:/c/ProgramData/chocolatey/lib/mingw/tools/install/mingw64/bin'
        - "travis_wait 50 mingw32-make"
        - cat distrib/Msys64-error.log
        - cat distrib/Msys64-build.log
    # - os: linux
      # dist: trusty
      # addons: 
        # apt:
          # sources: 
            # - ubuntu-toolchain-r-test
          # packages: 
            # - bsdtar gperf texinfo help2man gcc-multilib g++-multilib
      # script:
         # - ls -a "C:/Users/travis/build/Juppit/Minor-Tests/src"
# #         - "travis_wait 50 make"
    # - os: osx
      # script:
         # - ls -a "C:/Users/travis/build/Juppit/Minor-Tests/src"
# #         - "travis_wait 50 make"
# #    - os: osx
# #      script:
# #        - make build-2
before_script:
  - cd $TRAVIS_BUILD_DIR
  - mkdir -p distrib
after_script:
  - ls -la . distrib
on_failure:
  - tail -20 distrib/*error.log
before_install:
  - unset CC
  - unset CXX
deploy: 
  api_key: $CheckCIDeploy
  file: distrib/*
  file_glob: true
  overwrite: true
  provider: releases
  skip_cleanup: true
  true: 
    branches: 
      only: 
        - master
        - "/v\\d+\\.\\d+[a-z]/"
    repo: Juppit/check-travis
    tags: true
notifications:
  email: false
